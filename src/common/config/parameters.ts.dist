import {LogLevel} from "@nestjs/common";

export class Parameters {

    public db = {
        type: 'mysql',
        host: 'localhost',
        port: 3306,
        username: 'username',
        password: 'password',
        database: 'dbname',
    };

    public log: LogLevel[] = ['error', 'warn', 'log', 'debug', 'verbose']; // , 'verbose'];

    public jwtSecret = 'somesecrethere';

}
